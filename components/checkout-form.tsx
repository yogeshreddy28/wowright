'use client';
import{FormEvent,useMemo,useState}from'react';import{useRouter}from'next/navigation';import Link from'next/link';import{ArrowLeft,MessageCircle,ShieldCheck}from'lucide-react';import{AppShell}from'./app-shell';import{useStore}from'./store-provider';import{calculateCart,formatMoney}from'@/lib/services/pricing';import{defaultDeliveryConfig}from'@/lib/services/delivery';
const states=['Karnataka','Andhra Pradesh','Delhi','Goa','Gujarat','Kerala','Maharashtra','Tamil Nadu','Telangana','West Bengal','Other'];
export function CheckoutForm(){const store=useStore();const router=useRouter();const[loading,setLoading]=useState(false);const[error,setError]=useState('');const totals=useMemo(()=>calculateCart(store.items,defaultDeliveryConfig),[store.items]);async function submit(e:FormEvent<HTMLFormElement>){e.preventDefault();if(loading)return;setLoading(true);setError('');const form=new FormData(e.currentTarget);const customer=Object.fromEntries(form.entries());const idem=(sessionStorage.getItem('mm_checkout_key')||crypto.randomUUID());sessionStorage.setItem('mm_checkout_key',idem);try{const r=await fetch('/api/checkout',{method:'POST',headers:{'Content-Type':'application/json','Idempotency-Key':idem},body:JSON.stringify({sessionId:store.sessionId,items:store.items,customer})});const data=await r.json()as{orderNumber?:string;url?:string;error?:string};if(!r.ok)throw new Error(data.error||'We could not save your order');store.clear();store.notify('Order saved. Opening WhatsApp…');sessionStorage.removeItem('mm_checkout_key');router.push(`/order-success/${data.orderNumber}?wa=${encodeURIComponent(data.url||'')}`)}catch(e){setError(e instanceof Error?e.message:'Checkout failed. Please try again.')}finally{setLoading(false)}}if(!store.items.length)return<AppShell><section className="empty-state"><h1>Your cart is empty.</h1><p>Add a product before checking out.</p><Link className="button primary" href="/shop">Go to shop</Link></section></AppShell>;return<AppShell><section className="checkout-head"><Link href="/cart"><ArrowLeft/> Back to cart</Link><div><p className="eyebrow">Secure order capture</p><h1>Your details</h1><p>We save your complete order first, then open WhatsApp for payment or COD confirmation.</p></div></section><form className="checkout-layout" onSubmit={submit}><div className="form-card"><h2>Contact</h2><div className="field-grid"><label className="wide">Full name<input name="name" required autoComplete="name"/></label><label>Mobile number<input name="mobile" required inputMode="tel" placeholder="10-digit Indian mobile" autoComplete="tel"/></label><label>Email <small>optional</small><input name="email" type="email" autoComplete="email"/></label></div><h2>Delivery address</h2><div className="field-grid"><label className="wide">Address line 1<input name="line1" required autoComplete="address-line1"/></label><label className="wide">Address line 2 <small>optional</small><input name="line2" autoComplete="address-line2"/></label><label>Locality<input name="locality" required placeholder="e.g. JP Nagar"/></label><label>City<input name="city" required defaultValue="Bengaluru" autoComplete="address-level2"/></label><label>State<select name="state" defaultValue="Karnataka">{states.map(s=><option key={s}>{s}</option>)}</select></label><label>PIN code<input name="pinCode" required inputMode="numeric" pattern="[1-9][0-9]{5}" autoComplete="postal-code"/></label><label className="wide">Landmark <small>optional</small><input name="landmark"/></label><label className="wide">Delivery notes <small>optional</small><textarea name="notes"/></label></div></div><aside className="summary-card checkout-summary"><h2>Final summary</h2>{store.items.map(i=><div key={i.id}><span>{i.name} × {i.quantity}</span><b>{formatMoney(i.unitPrice*i.quantity)}</b></div>)}<hr/><div><span>Product subtotal</span><b>{formatMoney(totals.subtotal)}</b></div><div><span>Delivery</span><b>{formatMoney(totals.deliveryAmount)}</b></div><div className="summary-total"><span>Total</span><b>{formatMoney(totals.total)}</b></div>{error&&<p className="form-error">{error}</p>}<button className="button whatsapp full" disabled={loading} type="submit"><MessageCircle/>{loading?'Saving your order…':'Continue on WhatsApp'}</button><p className="secure-note"><ShieldCheck/> Server-verified pricing. No payment is collected on this page.</p></aside></form></AppShell>}
