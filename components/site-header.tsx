'use client';
import Link from'next/link';import{Menu,ShoppingBag,X}from'lucide-react';import{useState}from'react';import{useStore}from'./store-provider';
export function SiteHeader(){const{items}=useStore();const[open,setOpen]=useState(false);const count=items.reduce((s,i)=>s+i.quantity,0);return<header className="site-header"><Link className="brand" href="/">Morrow<span>Made</span></Link><nav aria-label="Primary navigation"><Link href="/shop">Shop</Link><Link href="/custom-print">Custom print</Link><Link href="/about">About</Link><Link href="/contact">Contact</Link></nav><div className="header-actions"><button className="menu-button" onClick={()=>setOpen(!open)} aria-label="Open menu">{open?<X/>:<Menu/>}</button><Link className="header-cart" href="/cart"><ShoppingBag size={18}/> Cart · {count}</Link></div>{open&&<nav className="mobile-menu"><Link onClick={()=>setOpen(false)} href="/shop">Shop products</Link><Link onClick={()=>setOpen(false)} href="/custom-print">Create something custom</Link><Link onClick={()=>setOpen(false)} href="/about">About</Link><Link onClick={()=>setOpen(false)} href="/contact">Contact</Link></nav>}</header>}
