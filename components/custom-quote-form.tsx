'use client';
import{FormEvent,useState}from'react';import{CheckCircle2,FileUp,Loader2}from'lucide-react';import{useStore}from'./store-provider';
export function CustomQuoteForm(){const[files,setFiles]=useState<File[]>([]);const[loading,setLoading]=useState(false);const[done,setDone]=useState('');const[error,setError]=useState('');const{sessionId}=useStore();async function submit(e:FormEvent<HTMLFormElement>){e.preventDefault();setLoading(true);setError('');const data=new FormData(e.currentTarget);data.set('sessionId',sessionId);files.forEach(f=>data.append('files',f));try{const r=await fetch('/api/custom-quotes',{method:'POST',body:data});const json=await r.json()as{requestNumber?:string;error?:string};if(!r.ok)throw new Error(json.error);setDone(json.requestNumber||'Saved')}catch(e){setError(e instanceof Error?e.message:'Upload failed')}finally{setLoading(false)}}if(done)return<div className="quote-success"><CheckCircle2/><p className="eyebrow">Request received</p><h2>We have your idea.</h2><p>Your reference is <b>{done}</b>. We’ll review the details and contact you—your files remain private.</p></div>;return<form className="quote-form" onSubmit={submit}><div className="field-grid"><label>Name<input name="name" required/></label><label>Mobile<input name="mobile" required inputMode="tel"/></label><label className="wide">Email <small>optional</small><input name="email" type="email"/></label><label className="wide">Describe your idea<textarea name="description" required rows={5} placeholder="What should it be, who is it for, and how will it be used?"/></label><label>Approximate dimensions<input name="dimensions" placeholder="e.g. 20 × 12 × 5 cm"/></label><label>Quantity<input name="quantity" type="number" min="1" defaultValue="1" required/></label><label>Desired colour<input name="desiredColour"/></label><label>Budget <small>optional</small><input name="budget" type="number" min="0" placeholder="₹"/></label><label>Required by <small>optional</small><input name="requiredBy" type="date"/></label><label>Additional notes<textarea name="notes"/></label></div><label className="upload-zone"><FileUp/><b>Add reference files</b><span>STL, 3MF, OBJ, STEP, JPG, PNG, WEBP or PDF · max 15 MB each</span><input type="file" multiple accept=".stl,.3mf,.obj,.step,.stp,.jpg,.jpeg,.png,.webp,.pdf" onChange={e=>setFiles(Array.from(e.target.files||[]))}/>{files.length>0&&<small>{files.map(f=>f.name).join(', ')}</small>}</label>{error&&<p className="form-error">{error}</p>}<button className="button primary full" disabled={loading}>{loading?<><Loader2 className="spin"/> Sending securely…</>:'Request a custom quote'}</button><p className="privacy-note">Files are stored privately and are only available to the production team.</p></form>}
