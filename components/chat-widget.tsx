'use client';
import{Bot,MessageCircle,Send,X}from'lucide-react';import{usePathname}from'next/navigation';import{FormEvent,useState}from'react';import{useStore}from'./store-provider';
type Msg={role:'customer'|'assistant';text:string};
export function ChatWidget(){const[open,setOpen]=useState(false);const[msgs,setMsgs]=useState<Msg[]>([]);const[input,setInput]=useState('');const[loading,setLoading]=useState(false);const path=usePathname();const{items,sessionId}=useStore();const greeting=path.includes('krishna')?'Hi! I can help you choose the size, colour and lighting for this Krishna idol.':path.includes('name-plate')?'I can help you customize your name plate. What name would you like on it?':'Hi! Tell me what you’re looking for and I’ll help you choose or customize it.';async function send(e:FormEvent){e.preventDefault();if(!input.trim()||loading)return;const text=input.trim();setInput('');setMsgs(x=>[...x,{role:'customer',text}]);setLoading(true);try{const r=await fetch('/api/ai/chat',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({message:text,sessionId,path,cart:items,history:msgs})});const d=await r.json()as{message:string};setMsgs(x=>[...x,{role:'assistant',text:d.message}])}catch{setMsgs(x=>[...x,{role:'assistant',text:'I’m having trouble connecting. You can keep shopping or talk to us on WhatsApp.'}])}finally{setLoading(false)}}async function human(){const r=await fetch('/api/whatsapp/handoff',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({sessionId,path,cart:items,messages:msgs})});const d=await r.json()as{url:string};window.open(d.url,'_blank','noopener,noreferrer')};return<><button className="chat-launcher" onClick={()=>setOpen(true)}><MessageCircle/> <span>Need help choosing?</span></button>{open&&<div className="chat-panel" role="dialog" aria-label="Product assistant"><header><div><Bot/><span><b>Product assistant</b><small>Here to help you choose</small></span></div><button onClick={()=>setOpen(false)} aria-label="Close assistant"><X/></button></header><div className="chat-messages"><div className="chat-bubble assistant">{greeting}</div>{msgs.map((m,i)=><div key={i} className={`chat-bubble ${m.role}`}>{m.text}</div>)}{loading&&<div className="typing"><i/><i/><i/></div>}</div><button className="human-link" onClick={human}>Talk to a person on WhatsApp</button><form onSubmit={send}><input value={input} onChange={e=>setInput(e.target.value)} placeholder="Ask about products, sizes or delivery…" aria-label="Message"/><button disabled={loading} aria-label="Send"><Send/></button></form></div>}</>}
