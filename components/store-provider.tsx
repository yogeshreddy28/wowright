'use client';
import{createContext,useContext,useEffect,useMemo,useState}from'react';import type{CartItem}from'@/lib/domain';
type Store={items:CartItem[];sessionId:string;add:(item:CartItem)=>void;remove:(id:string)=>void;quantity:(id:string,n:number)=>void;clear:()=>void;toast:string;notify:(s:string)=>void};
const Context=createContext<Store|null>(null);
export function StoreProvider({children}:{children:React.ReactNode}){const[items,setItems]=useState<CartItem[]>([]);const[sessionId,setSessionId]=useState('');const[toast,setToast]=useState('');useEffect(()=>{const stored=localStorage.getItem('mm_cart');if(stored)try{setItems(JSON.parse(stored))}catch{}let sid=localStorage.getItem('mm_session');if(!sid){sid=crypto.randomUUID();localStorage.setItem('mm_session',sid)}setSessionId(sid)},[]);useEffect(()=>{if(sessionId)localStorage.setItem('mm_cart',JSON.stringify(items))},[items,sessionId]);const notify=(s:string)=>{setToast(s);setTimeout(()=>setToast(''),2600)};const value=useMemo(()=>({items,sessionId,add:(item:CartItem)=>{setItems(x=>[...x,item]);notify('Added to cart')},remove:(id:string)=>{setItems(x=>x.filter(i=>i.id!==id));notify('Removed from cart')},quantity:(id:string,n:number)=>setItems(x=>x.map(i=>i.id===id?{...i,quantity:Math.max(1,n)}:i)),clear:()=>setItems([]),toast,notify}),[items,sessionId,toast]);return<Context.Provider value={value}>{children}{toast&&<div className="toast" role="status">{toast}</div>}</Context.Provider>}
export function useStore(){const value=useContext(Context);if(!value)throw new Error('StoreProvider missing');return value}
