import{describe,expect,it}from'vitest';import{getProduct}from'@/lib/catalog';import{calculateUnitPrice,calculateCart}from'@/lib/services/pricing';import{createOrderNumber}from'@/lib/services/orders';import{createWhatsAppOrderURL}from'@/lib/services/whatsapp';
describe('browse → customize → cart → order → WhatsApp',()=>{it('preserves server-verified totals through handoff',()=>{const product=getProduct('krishna-idol')!;const selections={size:'medium',colour:'white',lighting:'yes'};const{unitPrice}=calculateUnitPrice(product,selections);const totals=calculateCart([{unitPrice,quantity:1}],{baseCharge:100,freeThreshold:1999});const url=createWhatsAppOrderURL({orderNumber:createOrderNumber(1048),customer:{name:'Yogesh',mobile:'919353193080',line1:'1 Main Road',locality:'JP Nagar',city:'Bengaluru',state:'Karnataka',pinCode:'560078'},items:[{name:product.name,quantity:1,selections,unitPrice}],...totals,paymentStatus:'unpaid'});expect(unitPrice).toBe(1199);expect(totals.total).toBe(1299);expect(decodeURIComponent(url)).toContain('Total: ₹1,299')})});
