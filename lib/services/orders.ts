import type{OrderStatus}from'../domain';
export const transitions:Record<OrderStatus,OrderStatus[]>={draft:['checkout_started','cancelled'],checkout_started:['awaiting_confirmation','cancelled'],awaiting_confirmation:['confirmed','cancelled'],confirmed:['in_production','cancelled'],in_production:['ready','cancelled'],ready:['out_for_delivery','delivered','cancelled'],out_for_delivery:['delivered','ready'],delivered:[],cancelled:[]};
export function assertTransition(from:OrderStatus,to:OrderStatus){if(!transitions[from].includes(to))throw new Error(`Cannot move order from ${from} to ${to}`)}export function createOrderNumber(sequence?:number){const date=new Date().toISOString().slice(0,10).replaceAll('-','');return`3DP-${date}-${String(sequence??Math.floor(1000+Math.random()*9000))}`;}
