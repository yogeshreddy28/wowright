import type{CheckoutCustomer}from'../domain';import{formatMoney}from'./pricing';
export type WhatsAppOrder={orderNumber:string;customer:CheckoutCustomer;items:{name:string;quantity:number;selections:Record<string,string|number|boolean>;unitPrice:number}[];subtotal:number;deliveryAmount:number;total:number;paymentStatus:string};
export function createWhatsAppOrderMessage(order:WhatsAppOrder){const itemLines=order.items.map((item,i)=>{const options=Object.entries(item.selections).filter(([k])=>k!=='quantity').map(([k,v])=>`${k[0].toUpperCase()+k.slice(1)}: ${String(v)}`).join('\n');return`${i?'\n':''}Product: ${item.name}\n${options}${options?'\n':''}Quantity: ${item.quantity}\nAmount: ${formatMoney(item.unitPrice*item.quantity)}`}).join('\n');return`Hi, I'd like to confirm my order.\n\nOrder: #${order.orderNumber}\nCustomer: ${order.customer.name}\n\n${itemLines}\n\nProduct subtotal: ${formatMoney(order.subtotal)}\nDelivery: ${formatMoney(order.deliveryAmount)}\nTotal: ${formatMoney(order.total)}\n\nDelivery area: ${order.customer.locality}\nPayment status: Awaiting confirmation\n\nI'd like to confirm my payment/delivery option.`}
export function createWhatsAppOrderURL(order:WhatsAppOrder,number=process.env.WHATSAPP_BUSINESS_NUMBER||'919353193080'){const normalized=number.replace(/\D/g,'');if(!/^\d{10,15}$/.test(normalized))throw new Error('WhatsApp number is not configured');return`https://wa.me/${normalized}?text=${encodeURIComponent(createWhatsAppOrderMessage(order))}`}
export function createHumanHandoffURL(context:{sessionId:string;product?:string;selections?:Record<string,unknown>;summary?:string},number=process.env.WHATSAPP_BUSINESS_NUMBER||'919353193080'){const safeSession=context.sessionId.slice(0,8).toUpperCase();const selections=Object.entries(context.selections||{}).map(([k,v])=>`${k}: ${String(v)}`).join('\n');const message=`Hi, I need help with my order.\n\nReference: ${safeSession}${context.product?`\nProduct: ${context.product}`:''}${selections?`\n${selections}`:''}${context.summary?`\n\n${context.summary}`:''}\n\nI would like to talk to someone.`;return`https://wa.me/${number.replace(/\D/g,'')}?text=${encodeURIComponent(message)}`}
