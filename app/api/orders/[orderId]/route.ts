import{env}from'cloudflare:workers';export async function GET(_r:Request,{params}:{params:Promise<{orderId:string}>}){const{orderId}=await params;const order=await env.DB.prepare('SELECT id,order_number,status,payment_status,subtotal,delivery_amount,total,created_at FROM orders WHERE order_number = ?').bind(orderId).first<Record<string,unknown>>();if(!order)return Response.json({error:'Order not found'},{status:404});const items=await env.DB.prepare('SELECT id,product_name,quantity,unit_price,line_total FROM order_items WHERE order_id = ?').bind(order.id).all();return Response.json({order,items:items.results})}
