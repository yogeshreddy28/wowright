import{env}from'cloudflare:workers';import{verifyAdmin}from'@/lib/admin-auth';export async function GET(r:Request){if(!await verifyAdmin(r))return Response.json({error:'Unauthorized'},{status:401});const result=await env.DB.prepare('SELECT c.*, (SELECT message FROM conversation_messages WHERE conversation_id=c.id ORDER BY created_at DESC LIMIT 1) last_message,(SELECT COUNT(*) FROM conversation_messages WHERE conversation_id=c.id) message_count FROM conversations c ORDER BY updated_at DESC LIMIT 100').all();return Response.json({conversations:result.results})}
