import type{MetadataRoute}from'next';import{sampleProducts}from'@/lib/catalog';export default function sitemap():MetadataRoute.Sitemap{const base=process.env.SITE_URL||'http://localhost:3000';const routes=['','/shop','/custom-print','/about','/contact','/privacy','/terms'];return[...routes.map(path=>({url:`${base}${path}`,lastModified:new Date(),changeFrequency:path===''?'weekly' as const:'monthly' as const,priority:path===''?1:.7})),...sampleProducts.filter(p=>p.stockMode!=='quote_only').map(p=>({url:`${base}/product/${p.slug}`,lastModified:new Date(),changeFrequency:'weekly' as const,priority:.8}))]}
